import { BaseElement } from './base.element';

export class UserElement extends BaseElement {
    public firstName: string;
    public lastName: string;
    public accountId: string;
    public email: string;
    public phone: string;
}