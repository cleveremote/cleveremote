import { BaseElement } from './base.element';
import { SectorElement } from './sector.element';

export class NetworkElement extends BaseElement {
    public nodes: Array<any>;
    public links: Array<any>;
}